@using System.Data
@{
    DataTable dt = new DataTable();
    var queueSongs = ViewBag.Queue;
}


<div>
    @if (queueSongs != null)
    {
        @foreach (var song in queueSongs)
        {


            <div>
                <h2>@song</h2>
            </div>
        }

    }
</div>